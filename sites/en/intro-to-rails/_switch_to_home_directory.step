tip "If you are still in IRB, type `quit` to exit."

message "`cd` stands for **c**hange **d**irectory."

message "A directory is the same thing as a folder."

option_half "Windows" do
  console "cd c:\\Sites"
  message "`cd c:\\Sites` sets our Sites directory to our current directory."
end
option_half "Mac or Linux" do
  console "cd ~"
  message "`cd ~` sets our home directory to our current directory."
end
